export enum ActivityReviewStatus {
  NONE = 'none',
  FLAGGED = 'flagged',
  RESOLVED = 'resolved',
}

export enum ActivityFlagReason {
  SECURITY = 'security',
  BILLING = 'billing',
  COMPLIANCE = 'compliance',
  SYSTEM = 'system',
  OTHER = 'other',
}
